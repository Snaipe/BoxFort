--- _test.py.orig	2016-09-02 23:41:34.286781100 +0200
+++ "/c/Program Files/Python35/lib/site-packages/cram/_test.py"	2016-09-02 23:48:17.937263300 +0200
@@ -143,6 +143,8 @@
             after.setdefault(pos, []).append(line)
     stdin.append(b('echo %s %s $?\n' % (usalt, i + 1)))
 
+    env = {k: v.decode('utf-8') if isinstance(v, bytes) else v for k, v in env.items()}
+
     output, retcode = execute(shell + ['-'], stdin=b('').join(stdin),
                               stdout=PIPE, stderr=STDOUT, env=env)
     if retcode == 80:
